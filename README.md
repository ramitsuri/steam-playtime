# Play Pulse

React app that reads DeckyLoader PlayTime plugin data and generates charts from it.

## Run Locally

**Prerequisites:**  Node.js
1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
